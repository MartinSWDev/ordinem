# Ordinem orchestrator — configuration.
# Copy to `.env` and fill in. Nothing here is hardcoded in the app.

# --- Database (work schema) ---
# Local dev default matches the docker command in the README.
COOLIFY_POSTGRES_URL=postgresql://ordinem:ordinem@localhost:5433/ordinem
DB_SCHEMA=work

# --- Jira ---
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=you@example.com
JIRA_API_TOKEN=
# Custom field ID for "acceptance criteria" on your Jira instance, if used.
# Find via: GET /rest/api/3/field  (look for the field, note its "id", e.g. customfield_10034)
JIRA_ACCEPTANCE_CRITERIA_FIELD=

# --- Anthropic (Claude Agent SDK) ---
# Subscription-based SDK auth. Leave ANTHROPIC_API_KEY unset if using a
# logged-in Claude subscription session; set it for API-key auth.
ANTHROPIC_API_KEY=

# --- Qwen fallback (LiteLLM proxy over Tailscale) ---
# Used on a StopFailure (rate-limit / auth) event to re-dispatch a subtask.
QWEN_PROXY_URL=

# --- Server ---
HOST=127.0.0.1
PORT=8787
